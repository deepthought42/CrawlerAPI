package actors;

import org.testng.annotations.Test;

import com.minion.actors.FormDiscoveryActor;

import akka.actor.Props;

public class FormBuilderTests {
	@Test
	public void demonstrateTestActorRef() {
	  /*final Props props = new Props(FormDiscoveryActor.class);
	  final TestActorRef<FormDiscoveryActor> ref = TestActorRef.create(system, props, "testA");
	  final FormDiscoveryActor actor = ref.underlyingActor();
	  assertTrue(actor.testMe());
	  */
	}

}
